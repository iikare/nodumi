#pragma once

#include <string>

#include "build_target.h"
#include "color.h"
#include "wrap.h"

void drawNoteLabel(string label, int x, int y, int size, int sym_size,
                   const colorRGB& col);
